// Prisma schema for the Indian personal finance educational demo app.
// Models are added in Task 2 (DB schema + migrations).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
